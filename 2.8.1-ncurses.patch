* libncurses-devel provides a libcurses symlink

--- origsrc/cmake-2.8.1/Modules/FindCurses.cmake	2010-04-06 09:45:31.000000000 -0500
+++ src/cmake-2.8.1/Modules/FindCurses.cmake	2010-04-29 04:02:58.187387800 -0500
@@ -30,9 +30,9 @@ FIND_LIBRARY(CURSES_CURSES_LIBRARY NAMES
 FIND_LIBRARY(CURSES_NCURSES_LIBRARY NAMES ncurses )
 SET(CURSES_USE_NCURSES FALSE)
 
-IF(CURSES_NCURSES_LIBRARY  AND NOT  CURSES_CURSES_LIBRARY)
+IF(CURSES_NCURSES_LIBRARY)
   SET(CURSES_USE_NCURSES TRUE)
-ENDIF(CURSES_NCURSES_LIBRARY  AND NOT  CURSES_CURSES_LIBRARY)
+ENDIF(CURSES_NCURSES_LIBRARY)
 
 
 # Not sure the logic is correct here.
